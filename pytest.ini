[pytest]
testpaths = tests
pythonpath = . tests /home/tony/.hermes/hermes-agent
addopts = -ra --strict-markers
markers =
    gate_a: Gate A — enforcement semantics (SPEC-3)
    gate_b: Gate B — drift watchdog (SPEC-3)
timeout = 300
